To run it:
Python 3.11 is required 
and Node JS installed
First command is to run the backend Flask, and second one is to run React frontend part. 

`
cd backend
py -3.11 -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
python app.py`

``
cd frontend
npm install
npmrun dev
``


